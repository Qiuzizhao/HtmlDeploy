{
  "name": "html-deploy",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "migrate:sqlite": "node scripts/migrate-json-to-sqlite.js",
    "verify:sqlite": "node scripts/verify-sqlite-data.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "compression": "^1.8.1",
    "express": "^4.19.2",
    "multer": "^2.0.2",
    "playwright": "^1.60.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
